# Hermes chat preflight, 2026-09-25

- **Source:** pinned Hermes tag `v2026.9.7`, commit `2237be355906fbe6065ce1815711eee52b2d646e`, `uv sync --frozen`; API health reported `0.21.1`.
- **Topology:** disposable local Hermes gateway and synthetic model provider on loopback, separate `HERMES_HOME`; no personal content or credentials.
- **Configuration:** [`chat-config.yaml`](../../integrations/hermes/chat-config.yaml), `HERMES_SAFE_MODE=1`, `LUMEN_CHAT_ZERO_TOOL=1`, and the [pinned source patch](../../integrations/hermes/patches/0001-lumen-chat-zero-tool.patch). The profile disables auxiliary title generation.
- **Preflight:** [`check_chat_profile.py`](../../integrations/hermes/check_chat_profile.py) returned `preflight_passed`, zero constructed-agent tools, and no built-in or provider memory. Disabling safe mode or enabling memory caused a nonzero `unqualified` result.
- **Live Runs probe:** [`live_chat_probe.py`](../../integrations/hermes/live_chat_probe.py) started the patched source from a disposable home against a synthetic loopback provider. An API-key request with a forged tool-enabled room policy and a room-token request both returned denial. The model forced `terminal`, `session_search`, and memory-write calls: each run failed before tool execution, no side-effect marker appeared, and no tool-result follow-up reached the model. AGENTS/memory file sentinels were neither disclosed nor changed. Explicit canonical history on a second run excluded the prior session sentinel. An ordinary text run completed. Repeated invocations observed only the four expected model requests, no advertised tools, and no auxiliary title request.
- **Container probe:** the official archive matched SHA-256 `907c2a72db1c5dd637ea8eeae97f4cb5b32cef615c17258f6b190924ec5bf688`; `Dockerfile.hermes` applied the Lumen patch without fuzz and built local arm64 image ID `sha256:1be111fc52c371b9aba40bb2ea58fe175be96ae0aaef26a5d3bcb7862c1618d8`. [`container_chat_probe.py`](../../integrations/hermes/container_chat_probe.py) ran that image with a separate temporary home, read-only root and config mount, and synthetic provider. The three forced tool calls failed, room-token admission returned 403, ordinary chat completed, and only four no-tool model requests occurred. The image ID is local build evidence, not a distributable multi-platform release digest.
- **Rebuild and rerun, 2026-09-25:** after Docker Desktop restarted with an empty image store, the verified source archive was rebuilt with the same Dockerfile and patch. The new local arm64 image ID is `sha256:56965700fb5b5b2277457bf9e7ebaafbe363bdac6a561ff13f39aa45fd71c553`. The disposable container probe returned `container_probe_passed`, four provider requests, and three rejected tool calls. The changed image ID demonstrates that a source and patch match alone is not a deployed-image identity; any Host certificate must bind the exact image that was probed.
- **Result:** the disposable pinned source, config, and local container pass these bounded negative probes. The room-token case checks denial before grant validation, so a signed grant cannot enter the room dispatch path in this mode. Hermes still maintains replaceable session DB state; the source probe establishes no hidden replay when explicit Host history is provided, not absence of all runtime persistence. The probes do not give the Host trusted, fresh binding of a deployed process to its image, config, and endpoint or test real provider and restart journeys. **No production chat certificate was issued.**

## Local Host integration follow-up, 2026-09-26

The probe now reports image ID and exact config SHA-256. A development-only Host certifier checks the running read-only Docker container, its environment, loopback port, bearer-authenticated health, and the probe evidence before dispatch. `scripts/lumen-mac-chat-check` passed with a native macOS Host: ten canonical messages across five synthetic provider calls, Host and Hermes restarts, and memory save/delete changing subsequent context. Local image ID: `sha256:56965700fb5b5b2277457bf9e7ebaafbe363bdac6a561ff13f39aa45fd71c553`. This is local arm64 synthetic evidence. Production process-to-request binding, real-provider qualification, and manual owner acceptance remain open.
